
// This file is intentionally left empty as the Athkar Log feature is being removed.
// You can delete this file if it's no longer needed.
// Or, if you want a placeholder page:
/*
"use client";

import { useRouter } from 'next/navigation';
import { Button } from '@/components/ui/button';
import { ArrowRight } from 'lucide-react';

export default function AthkarLogPageRemoved() {
  const router = useRouter();
  return (
    <div dir="rtl" className="min-h-screen flex flex-col items-center justify-center p-4 bg-background text-foreground">
      <h1 className="text-2xl font-bold text-primary mb-6">
        تمت إزالة صفحة سجل الأذكار
      </h1>
      <p className="text-muted-foreground mb-8">
        هذه الخاصية غير متوفرة حاليًا.
      </p>
      <Button onClick={() => router.push('/')} variant="outline">
        <ArrowRight className="ml-2 rtl:mr-0 rtl:ml-2 h-4 w-4" />
        العودة إلى الرئيسية
      </Button>
    </div>
  );
}
*/
